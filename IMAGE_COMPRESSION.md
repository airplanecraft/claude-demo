# 图片压缩功能说明

## 概述

为了避免 API 请求过大导致的 Connection error，系统会在上传图片前自动压缩图片。

## 压缩策略

### 1. 目标大小
- **默认最大值**: 800 KB
- **初始质量**: 85%

### 2. 压缩流程

#### 阶段 1: 检查原始大小
如果原始图片 ≤ 800 KB:
- 使用质量 85% 进行轻微优化压缩
- 保持原始尺寸

#### 阶段 2: 调整质量
如果原始图片 > 800 KB:
1. 尝试质量参数: 85%, 70%, 55%, 40%, 25%
2. 每次降低 15% 质量
3. 最多尝试 5 次

#### 阶段 3: 缩小尺寸（如果质量调整不够）
1. 缩放比例: 80%, 70%, 60%, 50%, 40%
2. 使用高质量的 LANCZOS 重采样算法
3. 压缩质量固定为 70%
4. 最多尝试 5 次

### 3. 颜色模式处理

- **RGBA/LA/P 模式**: 自动转换为 RGB（白色背景）
- **其他模式**: 转换为 RGB
- **输出格式**: JPEG（最佳压缩效果）

## 压缩示例

### 示例 1: 小图片（500 KB）
```
原始大小: 500 KB
↓
轻微压缩 (质量 85%)
↓
最终大小: 420 KB
压缩率: 16%
```

### 示例 2: 中等图片（1.5 MB）
```
原始大小: 1536 KB (1920x1080)
↓
降低质量到 55%
↓
最终大小: 780 KB
压缩率: 49.2%
保持尺寸: 1920x1080
```

### 示例 3: 大图片（3 MB）
```
原始大小: 3072 KB (4096x2160)
↓
质量调整失败（仍 > 800KB）
↓
缩小尺寸到 70% (2867x1512)
压缩质量 70%
↓
最终大小: 765 KB
压缩率: 75.1%
```

## 日志输出

压缩过程会输出详细的日志信息：

```
2026-01-21 14:30:15 - INFO - 开始压缩图片: input/images/image-1.png
2026-01-21 14:30:15 - INFO - 原始图片大小: 1536.00 KB
2026-01-21 14:30:15 - INFO - 图片模式为 RGBA，转换为 RGB
2026-01-21 14:30:15 - INFO - 原始尺寸: 1920 x 1080
2026-01-21 14:30:15 - INFO - 图片大于 800KB，进行压缩处理
2026-01-21 14:30:15 - INFO - 尝试 1/5: 质量=85, 大小=920.45 KB
2026-01-21 14:30:15 - INFO - 尝试 2/5: 质量=70, 大小=815.23 KB
2026-01-21 14:30:15 - INFO - 尝试 3/5: 质量=55, 大小=780.12 KB
2026-01-21 14:30:15 - INFO - ✓ 压缩成功！最终大小: 780.12 KB
2026-01-21 14:30:15 - INFO - 压缩率: 49.2%
2026-01-21 14:30:15 - INFO - 使用压缩后的图片数据: 780.12 KB
2026-01-21 14:30:15 - INFO - Base64 编码完成，长度: 1067328 字符
```

## 配置参数

可以在 `run.py` 的 `compress_image()` 函数调用中修改参数：

```python
# 在 encode_image 函数中
image_data = compress_image(
    image_path,
    max_size_kb=800,   # 最大文件大小（KB）
    quality=85         # 初始压缩质量（1-100）
)
```

## 性能影响

- **压缩时间**: 通常 < 1 秒（取决于原始图片大小）
- **质量损失**: 在质量 55% 以上时，视觉效果基本无损
- **API 请求大小**: 显著减少，降低 Connection error 风险

## 故障处理

如果压缩过程失败：
1. 系统会记录警告日志
2. 自动回退到使用原始图片
3. 程序继续执行，不会中断

## 禁用压缩（不推荐）

如需禁用压缩功能，修改 `solve_math_problem` 函数：

```python
# 将 compress=True 改为 False
image_data = encode_image(image_path, compress=False)
```

⚠️ **注意**: 禁用压缩可能导致大图片上传失败或 Connection error。

## 图片格式支持

- **输入格式**: PNG, JPEG, GIF, WebP, BMP 等所有 PIL 支持的格式
- **输出格式**: JPEG（最佳压缩效果）
- **透明通道**: 自动转换为白色背景

## 最佳实践

1. **推荐图片大小**: 原始文件 < 2 MB
2. **推荐分辨率**: 1920x1080 或更低
3. **如果图片很大**:
   - 预先裁剪不必要的区域
   - 确保题目清晰可见
   - 避免包含过多空白区域

## 相关日志文件

所有压缩日志记录在 `run.log` 文件中，可以查看详细的压缩过程。
