# Manim 视频布局指南

本文档详细说明生成的 Manim 视频的布局要求和视觉效果。

## 视频分辨率

- **分辨率**：1920 × 1080 (1080p)
- **帧率**：60 FPS
- **背景色**：深绿色 (#415049)

## 视频结构

### 阶段一：封面（持续 3 秒）

封面阶段展示：
- **背景**：`assets/cover.png` 全屏显示
- **中央**：原题图片（4.0 × 3.0 尺寸）+ 题号标签（如 "Q"）
- **底部**：比赛信息文字（如：2025香港袋鼠数学竞赛中学中年级组）

```
┌─────────────────────────────────────────┐
│                                         │
│         [Cover 背景图全屏]              │
│                                         │
│           ┌──────────┐                  │
│        Q  │          │                  │
│           │  原题图  │                  │
│           │          │                  │
│           └──────────┘                  │
│                                         │
│      2025香港袋鼠数学竞赛中学中年级组   │
└─────────────────────────────────────────┘
```

### 阶段二：解题演示

```
┌────────────────────┬────────────────────┐
│ [Logo]             │                    │
│                    │                    │
│  ┌──────────┐      │  ┌──────────────┐ │
│  │          │      │  │              │ │
│  │          │      │  │  动画演示区  │ │
│  │  原题图  │   分  │  │  (右上方)    │ │
│  │          │   割  │  │              │ │
│  │          │   线  │  └──────────────┘ │
│  └──────────┘      │                    │
│                    │  ┌──────────────┐ │
│                    │  │ 步骤 1       │ │
│  ┌──────────┐      │  │ 步骤 2       │ │
│  │ 答案框   │      │  │ 步骤 3       │ │
│  └──────────┘      │  └──────────────┘ │
│                    │                    │
│ [语音字幕：当前步骤的语音内容]        │
└────────────────────┴────────────────────┘
```

## 关键布局坐标

### 左侧面板（原题区域）
- **中心 X 坐标**：`-3.5`
- **图片最大宽度**：`5.5`
- **图片最大高度**：`6.0`
- **答案显示位置**：`[-3.5, -3.2, 0]`

### 右侧面板（解题区域）
- **动画中心位置**：`POS_ANIM_CENTER = [3.5, 2.0, 0]`（右上）
- **文字基准位置**：`POS_TEXT_BASE = [3.5, -3.0, 0]`（右下）
- **文字行高**：`0.8`
- **同屏显示行数**：`3`（三个一组滚动）

### 分割线
- **X 坐标**：`0.0`
- **颜色**：白色 (#FFFFFF)
- **高度**：垂直贯穿（Y: -4 到 4）

### Logo
- **位置**：左上角（Upper Left）
- **缩放因子**：`0.15`
- **间距**：`0.1`

## 动画规则

### 必须遵守的规则

1. **动画位置**：
   - 所有动画对象必须使用 `.move_to(POS_ANIM_CENTER)` 定位
   - 动画区域：右上方（X = 3.5, Y = 2.0）
   - 建议尺寸：宽度 ≤ 4，高度 ≤ 3

2. **避免遮挡**：
   - 动画底部不得低于 Y = 0
   - 不得遮挡下方的解题步骤文字（Y ≈ -3.0）
   - 不得覆盖左侧的原题图片（X ≈ -3.5）

3. **步骤显示**：
   - 每个步骤调用 `self.play_rolling_step_text(step)`
   - 步骤会自动三个一组滚动显示
   - 旧步骤向上移动，新步骤从下方淡入

4. **音频处理**：
   - 每个步骤的音频会自动播放
   - 音频播放完毕后才进入下一步骤
   - 无需手动处理音频重叠问题

## 典型动画流程

```python
def play_visual_reasoning(self, steps):
    """AI 编写动画的典型结构"""

    for i, step in enumerate(steps):
        # 1. 先显示文字和播放语音
        self.play_rolling_step_text(step)

        # 2. 再播放对应的动画（在右上方区域）
        if i == 0:
            # 第一步：创建对象
            triangle = Triangle().scale(1.5)
            triangle.move_to(POS_ANIM_CENTER)  # 必须定位到右上方
            self.play(Create(triangle))

        elif i == 1:
            # 第二步：变换动画
            self.play(triangle.animate.rotate(PI/3))
            self.play_rolling_step_text(steps[i])

        elif i == 2:
            # 第三步：标注说明
            label = MathTex(r"\text{面积} = \frac{1}{2}bh")
            label.next_to(triangle, UP)
            self.play(Write(label))
            self.play_rolling_step_text(steps[i])

        # ... 更多步骤
```

## 颜色方案

- **背景深绿色**：`#415049`
- **网格深色**：`#253D32`
- **普通文本**：`#F0F5F2`（白灰色）
- **公式主色**：`#FFFFFF`（白色）
- **公式辅色**：`#A8D8B9`（浅绿色）
- **高亮色**：`#FFD700`（金黄色）- 用于重点步骤
- **字幕颜色**：`#FFFFFF`（白色）
- **字幕背景**：`#0A140F`（半透明黑）
- **分割线**：`#FFFFFF`（白色）

## 字体配置

- **字体名称**：`Heiti SC`（黑体 - Mac 系统）
- **正文大小**：`24`
- **公式大小**：`32`
- **字幕大小**：`24`
- **题号大小**：`48`

## 语音配置

- **TTS 引擎**：edge_tts（优先）或 Mac say 命令
- **语音**：zh-CN-XiaoxiaoNeural（edge_tts）或 Tingting（Mac）
- **语速**：+10%
- **字符阅读速度**：0.15 秒/字符
- **音频缓冲**：0.3 秒（防止重叠）

## 资源文件路径

```
claude-demo/
├── assets/
│   ├── logo.png          # Logo（左上角显示）
│   └── cover.png         # 封面背景（开头3秒）
├── input/
│   └── images/
│       └── image_1.png   # 原题图片（左侧显示）
└── output/
    └── image-1/
        └── image-1.py    # 生成的 Manim 代码
```

## 常见问题

### Q: 动画遮挡了解题步骤怎么办？
A: 确保所有动画对象使用 `.move_to(POS_ANIM_CENTER)` 定位，并且高度不超过建议值（≤ 3）。

### Q: 文字重叠怎么办？
A: 步骤文字会自动滚动，每次只显示 3 个步骤。无需手动处理。

### Q: 音频播放不连贯或重叠？
A: 使用 `self.play_rolling_step_text(step)` 方法，它会自动处理音频播放和等待时间。

### Q: 原题图片太大或太小？
A: 代码会自动缩放图片以适应左侧面板（最大宽度 5.5，最大高度 6.0）。

### Q: 如何显示数学公式？
A: 使用 `MathTex(r"\frac{a}{b}")`，支持完整的 LaTeX 语法和中文（ctex）。

## 示例视频时间轴

```
00:00 - 00:03  封面阶段（cover.png + 原题 + 题号）
00:03 - 00:04  过渡到解题界面
00:04 - 00:06  读题（高亮原题，播放读题语音）
00:06 - 02:30  解题演示（步骤1-N，每步包含文字+语音+动画）
02:30 - 02:33  显示最终答案
```

## 技术要求

- Python 3.9+
- Manim Community v0.17+
- edge_tts（语音合成）
- xelatex + ctex（中文支持）
- FFmpeg（视频输出）

---

**提示**：AI 在生成代码时，应严格遵守本文档中的布局规则，确保视频效果清晰、专业、无遮挡。
